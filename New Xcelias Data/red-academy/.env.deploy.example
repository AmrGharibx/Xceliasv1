# Copy to .env.deploy on YOUR server. Replace with a domain RED controls.
# No scheme, trailing slash, port, path, wildcard or spaces.
# Point its DNS at the server before requesting HTTPS certificates.
ACADEMY_DOMAIN=academy.your-red-domain.example
