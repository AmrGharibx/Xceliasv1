# RED Academy / 3.0.0

Migrated the supplied Notion export into the private Node/SQLite application. Retained all 42 real batches and related populated records; excluded demo batches and blank shells with explicit source decisions. Preserved original source pages, links, partial values, reports, remarks and discrepancies in an authenticated archive.

Added source-aware nullable historical schemas, multiple checklist/assessment records, shared and unassigned source records, cross-batch enrollment evidence, a full attendance register, source review workspace, history-safe editing, blank-safe score calculations, distinct reported/computed attendance and complete exports. No invented contacts, dates, grades, attendance or accounts.

Added independent source-field reconciliation, full-import browser workflows, imported HTTP/SSE/backup restoration and all-cell/formula workbook validation. Included a read-only archive verifier, non-overwriting reproducible importer and complete source inventory. The delivered database remains account-free; first owner setup and invitation-only access are unchanged.

Updated Docker instructions to bind-mount the already-populated private data directory; personal data is not baked into an image or seeded on startup. No live hosting or previous local installation was overwritten.

## Earlier 2.0 private edition

Removed guest/demo access, sample seed data, default credentials, public signup and browser-local operational records. Added private first-owner setup, invitations, session revocation, protected roles, persistent SQLite, server updates, backup/recovery and responsive RED interface. Version 3 retains those access controls.
